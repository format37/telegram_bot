version: '3.8'
services:
  server:
    restart: always
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
      - ./bots.json:/server/bots.json
      - ./config.json:/server/config.json
      - ./blocked.txt:/server/blocked.txt
      - /etc/letsencrypt/live/rtlm.info/fullchain.pem:/cert/fullchain.pem
      - /etc/letsencrypt/live/rtlm.info/privkey.pem:/cert/privkey.pem
    build: server
    container_name: bots_server_telegram
    logging:
      options:
        max-size: "10m"
        max-file: "3"
    network_mode: "host"
    environment:
      - TZ=Europe/Moscow